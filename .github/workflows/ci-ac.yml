name: ACs
on:
  pull_request:
    paths:
      - 'specs/features/**'
      - 'specs/spec_ledger.yaml'
      - 'crates/**'
      - 'crates/acceptance/**'
      - '.github/workflows/ci-ac.yml'

# Permissions for JUnit publisher action (publish-unit-test-result-action)
permissions:
  actions: read
  checks: write
  contents: read
  pull-requests: write
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  bdd:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
      - uses: cachix/install-nix-action@v31
      - name: Enable sccache
        run: |
          if nix develop -c sh -c 'command -v sccache >/dev/null 2>&1'; then
            echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
            echo "SCCACHE_GHA_ENABLED=1" >> $GITHUB_ENV
            echo "CARGO_INCREMENTAL=0" >> $GITHUB_ENV
            nix develop -c sccache --start-server || true
          else
            echo "Warning: sccache not found in nix devshell, skipping"
          fi
      - name: Run BDD tests via xtask
        env:
          ACTIONS_CACHE_URL: ${{ env.ACTIONS_CACHE_URL }}
          ACTIONS_RUNTIME_TOKEN: ${{ env.ACTIONS_RUNTIME_TOKEN }}
        run: nix develop -c cargo run -p xtask -- bdd

      - name: Generate AC status (Rust)
        if: always()
        env:
          ACTIONS_CACHE_URL: ${{ env.ACTIONS_CACHE_URL }}
          ACTIONS_RUNTIME_TOKEN: ${{ env.ACTIONS_RUNTIME_TOKEN }}
        run: nix develop -c cargo run -p xtask -- ac-status

      - name: Publish BDD JUnit results
        if: always()
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          files: target/junit/**/*.xml
      - name: Show sccache stats
        if: always()
        run: nix develop -c sccache --show-stats || true
      - name: Upload status
        if: always()
        uses: actions/upload-artifact@v4
        with: { name: feature-status, path: docs/feature_status.md }
  policy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31
      - name: AC Coverage Policy
        run: |
          nix develop -c bash -c 'yq -o=json specs/spec_ledger.yaml | conftest test -p policy/ledger.rego -'
