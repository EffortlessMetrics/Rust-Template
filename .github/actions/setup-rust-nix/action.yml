name: 'Setup Rust with Nix and caching'
description: 'Sets up Nix development environment with Rust, rust-cache, and sccache'
inputs:
  enable-sccache:
    description: 'Enable sccache for compilation caching'
    required: false
    default: 'true'
  enable-rust-cache:
    description: 'Enable rust-cache for dependency caching'
    required: false
    default: 'true'
  nix-installer:
    description: 'Nix installer action to use'
    required: false
    default: 'cachix'
outputs:
  cache-hit:
    description: 'Whether rust-cache had a cache hit'
    value: ${{ steps.rust-cache.outputs.cache-hit }}

runs:
  using: 'composite'
  steps:
    - name: Setup Rust cache
      if: inputs.enable-rust-cache == 'true'
      id: rust-cache
      uses: Swatinem/rust-cache@v2
      with:
        # Use shared key across all workflows to maximize cache hits
        shared-key: rust-${{ runner.os }}
        cache-on-failure: true

    - name: Install Nix (cachix)
      if: inputs.nix-installer == 'cachix'
      uses: cachix/install-nix-action@v31

    - name: Install Nix (determinate)
      if: inputs.nix-installer == 'determinate'
      uses: DeterminateSystems/nix-installer-action@v9

    - name: Configure Nix cache
      if: inputs.nix-installer == 'determinate'
      uses: DeterminateSystems/magic-nix-cache-action@v2

    - name: Enable sccache
      if: inputs.enable-sccache == 'true'
      shell: bash
      run: |
        # sccache is on PATH inside nix develop, so we just use the name directly.
        # This avoids issues with devshell welcome messages polluting path resolution.
        # Verify sccache is actually available before enabling.
        if nix develop -c sh -c 'command -v sccache >/dev/null 2>&1'; then
          echo "sccache found in nix devshell, enabling"
          echo "RUSTC_WRAPPER=sccache" >> $GITHUB_ENV
          echo "SCCACHE_GHA_ENABLED=1" >> $GITHUB_ENV
          echo "CARGO_INCREMENTAL=0" >> $GITHUB_ENV
          nix develop -c sccache --start-server || true
        else
          echo "Warning: sccache not found in nix devshell, skipping"
        fi

    - name: Show sccache stats (pre-build)
      if: inputs.enable-sccache == 'true'
      shell: bash
      run: nix develop -c sccache --show-stats || true
