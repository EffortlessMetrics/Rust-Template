## 2026-04-22 - Adding ARIA labels to search inputs
**Learning:** Search inputs must have an `aria-label` attribute; relying solely on a `placeholder` is considered an accessibility anti-pattern in this application's UI.
**Action:** Add aria-label attribute to all input elements that act as search boxes.
