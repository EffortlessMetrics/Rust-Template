## 2024-04-12 - Added ARIA label to search input

**Learning:** Forms without proper labels, specifically inputs that only rely on placeholders, are accessibility anti-patterns in this application's UI and must use `aria-label`.
**Action:** When creating new search inputs or modifying existing ones, ensure an `aria-label` is always present alongside the placeholder to provide context for screen reader users.
