
## 2026-05-03 - Semantic Search Inputs
**Learning:** Using `type="search"` instead of `type="text"` provides native browser features (like clear buttons) and better semantic meaning for assistive technologies, and an explicit `aria-label` ensures screen readers can correctly identify the input without external labels.
**Action:** Always use `type="search"` with an appropriate `aria-label` for search inputs.
