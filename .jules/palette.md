## 2024-04-01 - Added `aria-label` to search inputs
**Learning:** Relying solely on `placeholder` attributes for search inputs is a common accessibility anti-pattern because placeholders disappear when users type and aren't always reliably read by all screen readers.
**Action:** Always provide an explicit `aria-label` attribute for search inputs.
