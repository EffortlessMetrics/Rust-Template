## 2024-05-24 - Enhance search input accessibility
**Learning:** Adding the `aria-label` and `type="search"` to search inputs correctly enables the browser clear buttons, and provides full accessibility support to the interface.
**Action:** When a user interface requires a search capability, make sure the input types are `search` and include the appropriate `aria-label`.
