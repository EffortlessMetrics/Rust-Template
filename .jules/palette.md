# Palette's Journal

## 2026-01-22 - Server-Side Active Navigation
**Learning:** In `maud` templates, using `if/else` blocks is more robust for conditional attributes like `aria-current` than the `[condition]` syntax, especially when mixing with other attributes.
**Action:** When implementing active states in `maud`, prefer `if/else` branches for clarity and type safety.
