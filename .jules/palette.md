## $(date +%Y-%m-%d) - Native Search Inputs Provide Free UX Wins
**Learning:** Using `type="search"` instead of `type="text"` for search inputs automatically provides native browser features (like the clear "X" button) and better semantic meaning for screen readers.
**Action:** Always prefer `type="search"` with a descriptive `aria-label` for search fields instead of generic text inputs.
