[package]
name = "rust_iac_config"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Generic YAML-based infrastructure configuration library for Rust IaC projects"
repository = "https://github.com/EffortlessMetrics/Rust-Template"
keywords = ["iac", "infrastructure", "config", "yaml", "validation"]
categories = ["config", "development-tools"]

[lib]
doctest = false

[dependencies]
serde = { workspace = true }
serde_yaml = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3.23.0"
