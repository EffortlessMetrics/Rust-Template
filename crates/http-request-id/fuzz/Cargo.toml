[[bin]]
bench = false
doc = false
name = "request_id"
path = "fuzz_targets/request_id.rs"
test = false

[dependencies]
arbitrary = {features = ["derive"], version = "1"}
http-request-id = {path = ".."}
libfuzzer-sys = "0.4"

[package]
edition = "2024"
name = "http-request-id-fuzz"
publish = false
version = "0.0.0"

[package.metadata]
cargo-fuzz = true

[workspace]
members = ["."]
