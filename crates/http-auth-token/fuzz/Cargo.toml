[[bin]]
bench = false
doc = false
name = "extract_token"
path = "fuzz_targets/extract_token.rs"
test = false

[dependencies]
arbitrary = {features = ["derive"], version = "1"}
http = "1.4.0"
libfuzzer-sys = "0.4"

[dependencies.http-auth-token]
path = ".."

[package]
edition = "2024"
name = "http-auth-token-fuzz"
publish = false
version = "0.0.0"

[package.metadata]
cargo-fuzz = true

[workspace]
members = ["."]
