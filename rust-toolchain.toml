# This file is the source of truth for the Rust version used across the repository.
# All other configuration files should reference this version.

[toolchain]
channel = "1.92.0"
components = ["clippy", "llvm-tools-preview", "rustfmt"]
