# Rust Spec-as-Code Template

This repository is a **modern Rust DevEx** template: specs and policies as code, interface locks as blocking gates, reproducible dev envs, and supply-chain hygiene.

See `docs/` for architecture and `backstage/` for TechDocs+API Docs configuration.
